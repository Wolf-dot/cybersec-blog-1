<ul>
{% for post in collections.posts reversed %}
  <li>
    <span>step #{{post.order}} <a href="{{ post.url | url }}">{% if post.data.title %}{{ post.data.title }}{% else %}<code>{{ post.url }}</code>{% endif %}</a></span>
  </li>
{% endfor %}
</ul>